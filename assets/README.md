Place the application icon at:

```text
assets/3SD.ico
```

Recommended sizes inside the `.ico`: 16x16, 32x32, 48x48, and 256x256.

`assets/CartridgeLauncher.ico` is still accepted as a fallback during migration.
